<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!--
        Not used to draw an overlay (that approach was tried and abandoned).
        It is declared because holding it exempts the app from Android's
        background-activity-start restrictions, which is what lets the
        full-screen intent actually bring MainActivity up from a killed state.
        On Xiaomi/MIUI it is also the backing permission for "Display pop-up
        windows while running in background" — without this declaration that
        toggle does not even appear for the app, and MIUI silently downgrades
        every full-screen intent to a small banner.
    -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:label="Bloom"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!--
            showWhenLocked + turnScreenOn here are the manifest-level hints.
            MainActivity.kt also sets them programmatically via Activity APIs
            (API 27+) or window flags (API < 27) when it detects a notification
            launch, because the manifest attributes alone are insufficient on
            some OEMs (e.g. MIUI ignores them without the programmatic call).
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data android:name="flutterEmbedding" android:value="2" />
    </application>
</manifest>
